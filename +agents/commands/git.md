---
description: Save, stage, commit, and push all changes
---

1. `git add -A`
2. `git commit` with a concise message summarizing the changes
3. `git push`

Follow all git safety rules from `+agents/rules/AGENTS.md`.
